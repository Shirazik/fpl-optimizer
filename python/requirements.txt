# FPL Optimizer - Python Dependencies (local development)
#
# Matches api/requirements.txt so the local CLI script and the Vercel
# serverless function behave identically.
pulp>=2.8.0,<3.0
highspy>=1.5.0,<2.0
